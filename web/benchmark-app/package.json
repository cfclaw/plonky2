{
  "name": "plonky2-benchmark-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "cd ../plonky2-wasm-prover && wasm-pack build --target web --release --out-dir ../benchmark-app/src/wasm/cpu-prover && cd ../plonky2-webgpu-prover && wasm-pack build --target web --release --out-dir ../benchmark-app/src/wasm/webgpu-prover"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-wasm": "^3.3.0",
    "vite-plugin-top-level-await": "^1.4.0"
  }
}
